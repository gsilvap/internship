%!TEX root = main.tex

\DeclareAcronym{c01}{
  short = C01,
  long  = return value of the function \textbf{must not} being used,
  class = constraint
}
\DeclareAcronym{c02}{
  short = C02,
  long  = call \textbf{must not be} the only statement in the block,
  class = constraint
}
\DeclareAcronym{c03}{
  short = C03,
  long  = variable \textbf{must be} inside stack frame,
  class = constraint
}
\DeclareAcronym{c04}{
  short = C04,
  long  = \textbf{must be} the first assignment for that variable in the module,
  class = constraint
}
\DeclareAcronym{c05}{
  short = C05,
  long  = assignment \textbf{must not be} inside a loop,
  class = constraint
}
\DeclareAcronym{c06}{
  short = C06,
  long  = assignment \textbf{must not be} part of a for construct,
  class = constraint
}
\DeclareAcronym{c07}{
  short = C07,
  long  = \textbf{must not be} the first assignment for that variable in the module,
  class = constraint
}
\DeclareAcronym{c08}{
  short = C08,
  long  = the if construct \textbf{must not be} associated to an else construct,
  class = constraint
}
\DeclareAcronym{c09}{
  short = C09,
  long  = statements \textbf{must not include} more than five statements and not include loops,
  class = constraint
}
\DeclareAcronym{c10}{
  short = C10,
  long  = {statements are in the same block, \textbf{do not include} more than 5 stats, or loops},
  class = constraint
}
\DeclareAcronym{c11}{
  short = C11,
  long  = there \textbf{must be at least} two variables in this module,
  class = constraint
}

\DeclareAcronym{c08n}{
  short = C08n,
  long  = the if construct \textbf{must be} associated to an else construct,
  class = constraint
}
\DeclareAcronym{c12}{
  short = C12,
  long  = must have \textbf{at least two} branch conditions,
  class = constraint
}